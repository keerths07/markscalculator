# Project Constitution

This project follows Spec-Kit (Spec-Driven Development).

## Rules
- All features must be specified before implementation
- Maintain clean Streamlit architecture
- Backend logic must stay separate from UI
- Avoid duplicate UI elements (Streamlit keys required)