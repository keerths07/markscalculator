# Spec-Kit Configuration

This project follows Spec-Driven Development.

Rules:
- Every feature should have a spec in /specs
- All changes must be traceable to a spec
- Specs define requirements before coding